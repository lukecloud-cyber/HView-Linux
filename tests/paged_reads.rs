/*
This integration target compiles the bounded paged component before viewer integration.
The component keeps its focused checks beside the implementation.
L02.2 will connect the accepted component to the application lifecycle.
*/
#[path = "../src/paged.rs"]
mod paged;
